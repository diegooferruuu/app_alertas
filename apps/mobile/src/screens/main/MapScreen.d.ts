import React from 'react';

// Declaración de tipo para que TypeScript resuelva el import.
// En runtime, Metro elige MapScreen.native.tsx (iOS/Android) o MapScreen.web.tsx (web).
export declare const MapScreen: React.FC<{ navigation: any }>;
